services:
  - name: fastapi-app
    type: web
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: startCommand: uvicorn my_api.app:app --host 0.0.0.0 --port 10000
    plan: free
